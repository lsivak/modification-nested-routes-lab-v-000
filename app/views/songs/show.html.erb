<h1><%= @song.title %></h1>

<p>by <%= link_to @song.artist.name, artist_path(@song.artist) if @song.artist %> (<%= link_to "Edit Post", edit_artist_song_path(@song.artist, @song) if @song.artist %>)</p>

<h3> by <%= display_artist(@song) %></h3>

<%= link_to "All Songs", songs_path %>
